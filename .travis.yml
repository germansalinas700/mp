language: java

dist: xenial

services:
  - xvfb

env:
  global:
    - MOZ_HEADLESS=1
addons:
  firefox: "latest"
  
script: mvn clean test

before_install:
  - chmod +x /home/travis/build/germansalinas700/mp/geckodriver
